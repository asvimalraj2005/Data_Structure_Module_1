# Printing linked list 
# class Node:
#       def __init__(self,newData):
#              self.data=newData
#              self.next=None
# def PrintList(head):
#       while head is not None:
#               print(head.data,end=" ")           # Accessing the node data by using head.data 
#               head = head . next                 # Iterating over the nodes 

# The above is used to print the entire linked list while maintaining the order and without modifying the node pointers 
